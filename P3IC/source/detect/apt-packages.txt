# Debian/Ubuntu packages required to build and run the project
# Install with: sudo xargs -a apt-packages.txt apt-get install -y
build-essential
cmake
pkg-config
libpng-dev
libjpeg-dev
zlib1g-dev
python3
python3-pip
python3-venv
git

# Optional but useful for faster builds / profiling
# - clang, lcov, valgrind, gdb
